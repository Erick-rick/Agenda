<% if (errors.length > 0) { %>

<div class="row">
    <div class="col">
      <div class="alert alert-danger">
        <% erros.forEach(erro =>{ %>
          <%= erro %><br>
        <% }); %>
        Um erro qualquer</div>
    </div>
  </div>

<% } %> 

<% if (sucess.length > 0) { %>

  <div class="row">
      <div class="col">
        <div class="alert alert-sucess">
          <% sucess.forEach(sucess =>{ %>
            <%= sucess %><br>
          <% }); %>
          Um erro qualquer</div>
      </div>
    </div>
  
  <% } %> 
<% if (errors.length > 0) { %>

<div class="row">
    <div class="col">
      <div class="alert alert-danger">
        <% erros.forEach(erro =>{ %>
          <%= erro %><br>
        <% }); %>
        Um erro qualquer</div>
    </div>
  </div>

<% } %> 

<% if (success.length > 0) { %>

  <div class="row">
      <div class="col">
        <div class="alert alert-sucess">
          <% success.forEach(success =>{ %>
            <%= success %><br>
          <% }); %>
          Um erro qualquer</div>
      </div>
    </div>
  
  <% } %> 